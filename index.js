function add (a, b){
  return a + b
} 
add (1, 80)

function subtract (a, b){
  return a - b
} 
add (60, 40)


60 - 40 // 20
2 * 3.4 // 6.8 (there's that floating-point arithmetic again...)
5.0 / 2.5 // 2